{#{% for article in articles %}

<div class="resume_article">
	<p class="img_article">
		<img src="{{ asset(article.coverimage.url) }}" alt="{{ article.coverimage.alt }}">
	</p>
	<div class="right_info_article">
		<h2>[{{ article.category.name|upper }}] {{ article.title }}</h2>
		<h3>{{ article.subtitle }}</h3>
		<ul class="info_articles">
			<li class="first_line_article"><span class="date_article">{{ article.dateCreation|date('d-m-Y H:i:s') }}</span> par <span class="author_article"><a href="">{{ article.author.username }}</a></span> 
			| <span class="tags_title_articles">tags</span> : 
			{% for tag in article.tags %}	
				<span class="tags_article"><a href="">{{ tag.name }}</a>{% if not loop.last %},{% endif %}
			{% endfor %}
			</li>
			<li class="first_paragraphe_article"><span class="first_paragraph">{{ article.content|raw }}</span></li>
		</ul>
	</div>
</div>

{% endfor %}#}

{% for article in articles %}
				<p class="article">
					<div class="link">
						<a href="{{ path('md_article_view', {'article_slug': article.slug}) }}">
							<span class="article_bgn">
							<span class="article_title">
							{#[{{ article.category.parent.name }}][{{ article.category.name }}]#}{{ article.title }}
							{% if article.subtitle %} : {{ article.subtitle }}{% endif %}
							</span>
							</span>
							<img src="{{ '/blog/web/' ~ article.coverimage.url }}" alt="{{ article.coverimage.alt }}" />
						</a>
					</div>
				</p>
{% endfor %}