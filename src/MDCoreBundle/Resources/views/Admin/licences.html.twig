{% extends "MDCoreBundle::layoutadmin.html.twig" %}

{% block title %}administration de licences{% endblock %}

{% block body %}

<p><a href="{{ path('md_core_game_licence_create') }}">Ajouter une licence</a></p>

<div id="list_functions">

<ul>
{% for l in listobjects %}
<li><a href="{{ path('md_core_game_licence_view', {'id': l.id}) }}">{{ l.name }}</a> <a href="{{ path ('md_core_game_licence_edit', {'id': l.id}) }}">edit</a>
{% if is_granted('ROLE_ADMIN') %}<a href="{{ path('md_core_game_licence_delete', {'id': l.id}) }}">delete</a>{% endif %}
</li>
{% endfor %}
</ul>

<ul>
{% for page in paging %}
<li><a href="{{ path('md_core_admin_game_licences', {'batch': batch, 'page': page}) }}">{{ page }}</a></li>
{% endfor %}
</ul>

</div>
{% endblock %}
