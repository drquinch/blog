{% extends "MDCoreBundle::layout.html.twig" %}

{% block title %}{% endblock %}

{% block body %}
    {{ render(controller("MDGameBundle:Publisher:view", {'id': id})) }}
    <p>
    <a href="{{ path('md_core_home') }}">Retour aux publisher</a><br />
    <a href="{{ path('md_core_publisher_edit') }}">Modifier</a><br />
    <a href="{{ path('md_core_publisher_delete') }}">Supprimer</a><br />
    </p>
{% endblock %}
