{% extends '::layoutbackend.html.twig' %}

{% block title %}
Suppression comment
{% endblock %}

{% block main %}
    {% if form is defined %}
        <h2>Suppression comment</h2>
        <p>Voulez-vous supprimer TODO mettre title/name/etc.</p>
        <form action="{{ path('md_core_comment_delete', {'id': {ctrlname,,}.id}) }}" method="post">
            <a href="{{ path('md_core_comment_view', {'id': comment.id}) }}">Retour vers comment</a>
            <input type="submit" value="Supprimer" />
            {{ form(form) }}
        </form>
    {% else %}

    {% endif %}
{% endblock %}
