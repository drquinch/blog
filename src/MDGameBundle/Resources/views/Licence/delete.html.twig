{# Must be included in the core bundle #}

{% block title %}
Suppression licence
{% endblock %}

{% block body %}
    {% if form is defined %}
        <h2>Suppression licence</h2>
        <p>Voulez-vous supprimer TODO mettre title/name/etc.</p>
        <form action="{{ path('md_core_licence_delete', {'id': {ctrlname,,}.id}) }}" method="post">
            <a href="{{ path('md_core_licence_view', {'id': licence.id}) }}">Retour vers licence</a>
            <input type="submit" value="Supprimer" />
            {{ form(form) }}
        </form>
    {% else %}

    {% endif %}
{% endblock %}
