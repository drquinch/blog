{# Must be included in the core bundle #}

{% block title %}
Suppression genre
{% endblock %}

{% block body %}
    {% if form is defined %}
        <h2>Suppression genre</h2>
        <p>Voulez-vous supprimer TODO mettre title/name/etc.</p>
        <form action="{{ path('md_core_genre_delete', {'id': {ctrlname,,}.id}) }}" method="post">
            <a href="{{ path('md_core_genre_view', {'id': genre.id}) }}">Retour vers genre</a>
            <input type="submit" value="Supprimer" />
            {{ form(form) }}
        </form>
    {% else %}

    {% endif %}
{% endblock %}
