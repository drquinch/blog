{# Must be included in the core bundle #}

{% if form is defined %}
    {{ include("MDGameBundle:Genre:form.html.twig") }}
{% endif %}

{% if genre is defined %}
	{{ include("MDGameBundle:Genre:linksSendedForm.html.twig") }}
{% endif %}
