{
	"elements":[
		{% for publisher in publishers %}
			{% if loop.last %}
				{"element": "{{ publisher.name }}"}
			{% else %}
				{"element": "{{ publisher.name }}"},
			{% endif %}
		{% endfor %}]
}