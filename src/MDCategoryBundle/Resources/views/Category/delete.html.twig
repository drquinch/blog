{% extends '::layoutbackend.html.twig' %}

{% block title %}
Suppression category
{% endblock %}

{% block main %}
    {% if form is defined %}
        <h2>Suppression category</h2>
        <p>Voulez-vous supprimer TODO mettre title/name/etc.</p>
        <form action="{{ path('md_core_category_delete', {'id': {ctrlname,,}.id}) }}" method="post">
            <a href="{{ path('md_core_category_view', {'id': category.id}) }}">Retour vers category</a>
            <input type="submit" value="Supprimer" />
            {{ form(form) }}
        </form>
    {% else %}

    {% endif %}
{% endblock %}
